import { Double } from "react-native/Libraries/Types/CodegenTypes"

export interface MenuItemType {
    name: string
    price: Double
    description: string
    category: string
    quantity: number
}